{
  "name": "aspire-service-defaults",
  "description": "Shared MCP context for aspire1.ServiceDefaults - Used by ALL agents",
  "version": "1.0.0",
  "criticalScope": true,
  "tools": [
    {
      "name": "get-shared-architecture",
      "description": "Returns architecture for shared ServiceDefaults library",
      "inputSchema": {
        "type": "object",
        "properties": {
          "section": {
            "type": "string",
            "enum": ["health-checks", "tracing", "metrics", "resilience", "all"],
            "default": "all"
          }
        }
      }
    },
    {
      "name": "get-all-service-dependencies",
      "description": "Returns how all services use ServiceDefaults",
      "inputSchema": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "validate-breaking-change",
      "description": "CRITICAL: Validates that changes don't break dependent services",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filePath": {
            "type": "string",
            "description": "File being modified in ServiceDefaults"
          },
          "changeDescription": {
            "type": "string",
            "description": "Description of the breaking change"
          }
        },
        "required": ["filePath", "changeDescription"]
      }
    }
  ]
}
